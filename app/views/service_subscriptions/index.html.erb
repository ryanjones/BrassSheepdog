<h1>Listing service_subscriptions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Enabled</th>
  </tr>

<% @service_subscriptions.each do |service_subscription| %>
  <tr>
    <td><%=h service_subscription.service.name %></td>
    <td><%=h service_subscription.enabled %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add/Remove Service Subscriptions', services_path %>