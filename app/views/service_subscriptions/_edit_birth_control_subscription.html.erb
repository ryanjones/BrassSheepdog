<div id="birth-control-set">
	<div class="setting-set-title">Configure the birth control options below</div>
	<div class="setting-set-hint">Reminders will be sent based on the following criteria.</div>
	<div class="setting-set">
		<div class="day-box">
			<div class="form">
				<div class="pill-length">
					What type of birth control are you on? 
					<%= f.select("pill_length", %w[21 28]) %> day cycle.
				</div>
				<div class="pill-current-day">
					Which pill # did you take TODAY? 
					<%= f.select("pill_day", (1..28).to_a) %>
				</div>
				<div class="pill-reminder-time">
					Alert Time: <%= f.time_select :pill_delivery_time, :minute_step => 30, :twelve_hour => true, :default => Time.parse("7:00 a.m.").to_datetime%>
				</div>
			</div>
		</div>
	</div>
</div>
