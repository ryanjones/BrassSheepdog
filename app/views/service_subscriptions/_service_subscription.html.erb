<li>
	<span class="name"><%= h service_subscription.service.display_name %></span>
	<span class="enabled"><%= service_subscription.enabled ? "Enabled" : "Disabled" %></span>
	<span class="actions"><%= link_to "Edit Settings", edit_service_subscription_path(service_subscription) %> | <%= link_to "Unsubscribe", service_subscription_path(service_subscription), :method => :delete %></span>
	
</li>